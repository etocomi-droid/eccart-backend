import {
  useProductTypeTableColumns
} from "./chunk-4ETSAEHC.js";
import {
  getFormattedShippingOptionLocationName
} from "./chunk-HZVSE7W2.js";
import {
  TextCell
} from "./chunk-OKQK23FQ.js";
import {
  useShippingOptionTableFilters
} from "./chunk-MFWO5BNX.js";
import {
  useProductTypeTableFilters
} from "./chunk-OCJTBUNV.js";
import {
  useShippingOptionTableQuery
} from "./chunk-7XWUZBZU.js";
import {
  useProductTypeTableQuery
} from "./chunk-ITWRYKT3.js";
import {
  useProductTableColumns
} from "./chunk-K6D45I54.js";
import {
  DateCell
} from "./chunk-533U3J7O.js";
import {
  _DataTable,
  useDataTable
} from "./chunk-IFR4MV6Z.js";
import {
  useProductTableFilters
} from "./chunk-DG6YP2VU.js";
import {
  useProductTableQuery
} from "./chunk-7EX57PE7.js";
import {
  StackedDrawer,
  StackedFocusModal
} from "./chunk-4WOA2IC2.js";
import {
  external_exports
} from "./chunk-4E3X3L63.js";
import {
  useProductTypes
} from "./chunk-F2E4KAQU.js";
import {
  useShippingOptions
} from "./chunk-ON7O6BD7.js";
import {
  useStockLocations
} from "./chunk-BI2LAXJF.js";
import {
  useProduct,
  useProducts
} from "./chunk-I4PQNHCO.js";
import {
  keepPreviousData
} from "./chunk-R35JBZ3G.js";
import {
  useSearchParams
} from "./chunk-T7YBVUWZ.js";
import {
  useTranslation
} from "./chunk-LR2HKGOA.js";
import {
  Button,
  Checkbox,
  IconButton,
  Text,
  XMarkMini,
  createColumnHelper
} from "./chunk-ANJVOWZ3.js";
import {
  require_jsx_runtime
} from "./chunk-KBTYAULA.js";
import {
  require_react
} from "./chunk-QCHXOAYK.js";
import {
  __toESM
} from "./chunk-WOOG5QLI.js";

// node_modules/@medusajs/dashboard/dist/chunk-HNJ65IND.mjs
var import_react = __toESM(require_react(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var TaxRateRuleReferenceType = ((TaxRateRuleReferenceType2) => {
  TaxRateRuleReferenceType2["PRODUCT"] = "product";
  TaxRateRuleReferenceType2["PRODUCT_TYPE"] = "product_type";
  TaxRateRuleReferenceType2["SHIPPING_OPTION"] = "shipping_option";
  return TaxRateRuleReferenceType2;
})(TaxRateRuleReferenceType || {});
var columnHelper = createColumnHelper();
var useShippingOptionTableColumns = () => {
  const { t } = useTranslation();
  return (0, import_react2.useMemo)(
    () => [
      columnHelper.accessor("name", {
        header: () => t("fields.name"),
        cell: ({ getValue }) => (0, import_jsx_runtime.jsx)(TextCell, { text: getValue() })
      }),
      columnHelper.accessor("shipping_profile", {
        header: () => t("fields.shippingProfile"),
        cell: ({ row }) => {
          var _a;
          return (0, import_jsx_runtime.jsx)(TextCell, { text: ((_a = row.original.shipping_profile) == null ? void 0 : _a.name) || "N/A" });
        }
      }),
      columnHelper.display({
        id: "location",
        header: () => t("fields.location"),
        cell: ({ row }) => {
          const locationName = getFormattedShippingOptionLocationName(
            row.original
          );
          return (0, import_jsx_runtime.jsx)(TextCell, { text: locationName });
        }
      }),
      columnHelper.display({
        id: "service_zone",
        header: () => t("fields.serviceZone"),
        cell: ({ row }) => {
          var _a;
          const serviceZoneName = (_a = row.original.service_zone) == null ? void 0 : _a.name;
          return (0, import_jsx_runtime.jsx)(TextCell, { text: serviceZoneName || "N/A" });
        }
      }),
      columnHelper.display({
        id: "enabled_in_store",
        header: () => t("fields.enabledInStore"),
        cell: ({ row }) => {
          var _a;
          let text = "N/A";
          const val = (_a = row.original.rules) == null ? void 0 : _a.find(
            (r) => r.attribute === "enabled_in_store"
          );
          if (val) {
            text = val.value === "true" ? "Yes" : "No";
          }
          return (0, import_jsx_runtime.jsx)(TextCell, { text });
        }
      }),
      columnHelper.display({
        id: "is_return",
        header: () => t("fields.isReturn"),
        cell: ({ row }) => {
          var _a;
          let text = "N/A";
          const val = (_a = row.original.rules) == null ? void 0 : _a.find(
            (r) => r.attribute === "is_return"
          );
          if (val) {
            text = val.value === "true" ? "Yes" : "No";
          }
          return (0, import_jsx_runtime.jsx)(TextCell, { text });
        }
      }),
      columnHelper.accessor("created_at", {
        header: () => t("fields.createdAt"),
        cell: ({ getValue }) => {
          return (0, import_jsx_runtime.jsx)(DateCell, { date: getValue() });
        }
      })
    ],
    [t]
  );
};
function initRowSelection(state) {
  return state.reduce((acc, reference) => {
    acc[reference.value] = true;
    return acc;
  }, {});
}
var TargetForm = ({
  referenceType,
  type,
  setState,
  state
}) => {
  const { t } = useTranslation();
  const Component = type === "focus" ? StackedFocusModal : StackedDrawer;
  const [intermediate, setIntermediate] = (0, import_react.useState)(state);
  const handleSave = () => {
    setState(intermediate);
  };
  return (0, import_jsx_runtime2.jsxs)("div", { className: "flex size-full flex-col overflow-hidden", children: [
    (0, import_jsx_runtime2.jsx)(Component.Body, { className: "min-h-0 p-0", children: (0, import_jsx_runtime2.jsx)(
      Table,
      {
        referenceType,
        initialRowState: initRowSelection(state),
        intermediate,
        setIntermediate
      }
    ) }),
    (0, import_jsx_runtime2.jsxs)(Component.Footer, { children: [
      (0, import_jsx_runtime2.jsx)(Component.Close, { asChild: true, children: (0, import_jsx_runtime2.jsx)(Button, { variant: "secondary", size: "small", type: "button", children: t("actions.cancel") }) }),
      (0, import_jsx_runtime2.jsx)(Button, { type: "button", size: "small", onClick: handleSave, children: t("actions.save") })
    ] })
  ] });
};
var Table = ({ referenceType, ...props }) => {
  switch (referenceType) {
    case "product":
      return (0, import_jsx_runtime2.jsx)(ProductTable, { ...props });
    case "product_type":
      return (0, import_jsx_runtime2.jsx)(ProductTypeTable, { ...props });
    case "shipping_option":
      return (0, import_jsx_runtime2.jsx)(ShippingOptionTable, { ...props });
    default:
      return null;
  }
};
var PAGE_SIZE = 50;
var cgColumnHelper = createColumnHelper();
var PREFIX_PRODUCT = "p";
var ProductTable = ({
  initialRowState,
  intermediate,
  setIntermediate
}) => {
  const { t } = useTranslation();
  const [rowSelection, setRowSelection] = (0, import_react.useState)(initialRowState);
  useCleanupSearchParams();
  const { searchParams, raw } = useProductTableQuery({
    pageSize: PAGE_SIZE,
    prefix: PREFIX_PRODUCT
  });
  const { products, count, isLoading, isError, error } = useProducts(
    searchParams,
    {
      placeholderData: keepPreviousData
    }
  );
  const updater = (value) => {
    const state = typeof value === "function" ? value(rowSelection) : value;
    const currentIds = Object.keys(rowSelection);
    const ids = Object.keys(state);
    const newIds = ids.filter((id) => !currentIds.includes(id));
    const removedIds = currentIds.filter((id) => !ids.includes(id));
    const newProducts = (products == null ? void 0 : products.filter((p) => newIds.includes(p.id)).map((p) => ({
      value: p.id,
      label: p.title
    }))) || [];
    const filteredIntermediate = intermediate.filter(
      (p) => !removedIds.includes(p.value)
    );
    setIntermediate([...filteredIntermediate, ...newProducts]);
    setRowSelection(state);
  };
  const filters = useProductTableFilters();
  const columns = useProductColumns();
  const { table } = useDataTable({
    data: products || [],
    columns,
    count,
    enablePagination: true,
    enableRowSelection: true,
    getRowId: (row) => row.id,
    rowSelection: {
      state: rowSelection,
      updater
    },
    pageSize: PAGE_SIZE,
    prefix: PREFIX_PRODUCT
  });
  if (isError) {
    throw error;
  }
  return (0, import_jsx_runtime2.jsx)(
    _DataTable,
    {
      table,
      columns,
      pageSize: PAGE_SIZE,
      count,
      isLoading,
      filters,
      orderBy: [
        { key: "title", label: t("fields.title") },
        { key: "created_at", label: t("fields.createdAt") },
        { key: "updated_at", label: t("fields.updatedAt") }
      ],
      layout: "fill",
      pagination: true,
      search: true,
      prefix: PREFIX_PRODUCT,
      queryObject: raw
    }
  );
};
var pColumnHelper = createColumnHelper();
var useProductColumns = () => {
  const base = useProductTableColumns();
  return (0, import_react.useMemo)(
    () => [
      pColumnHelper.display({
        id: "select",
        header: ({ table }) => {
          return (0, import_jsx_runtime2.jsx)(
            Checkbox,
            {
              checked: table.getIsSomePageRowsSelected() ? "indeterminate" : table.getIsAllPageRowsSelected(),
              onCheckedChange: (value) => table.toggleAllPageRowsSelected(!!value)
            }
          );
        },
        cell: ({ row }) => {
          return (0, import_jsx_runtime2.jsx)(
            Checkbox,
            {
              checked: row.getIsSelected(),
              onCheckedChange: (value) => row.toggleSelected(!!value),
              onClick: (e) => {
                e.stopPropagation();
              }
            }
          );
        }
      }),
      ...base
    ],
    [base]
  );
};
var pcColumnHelper = createColumnHelper();
var PREFIX_PRODUCT_TYPE = "pt";
var ProductTypeTable = ({
  initialRowState,
  intermediate,
  setIntermediate
}) => {
  const { t } = useTranslation();
  const [rowSelection, setRowSelection] = (0, import_react.useState)(initialRowState);
  useCleanupSearchParams();
  const { searchParams, raw } = useProductTypeTableQuery({
    pageSize: PAGE_SIZE,
    prefix: PREFIX_PRODUCT_TYPE
  });
  const { product_types, count, isLoading, isError, error } = useProductTypes(
    searchParams,
    {
      placeholderData: keepPreviousData
    }
  );
  const updater = (value) => {
    const state = typeof value === "function" ? value(rowSelection) : value;
    const currentIds = Object.keys(rowSelection);
    const ids = Object.keys(state);
    const newIds = ids.filter((id) => !currentIds.includes(id));
    const removedIds = currentIds.filter((id) => !ids.includes(id));
    const newTypes = (product_types == null ? void 0 : product_types.filter((p) => newIds.includes(p.id)).map((p) => ({
      value: p.id,
      label: p.value
    }))) || [];
    const filteredIntermediate = intermediate.filter(
      (p) => !removedIds.includes(p.value)
    );
    setIntermediate([...filteredIntermediate, ...newTypes]);
    setRowSelection(state);
  };
  const filters = useProductTypeTableFilters();
  const columns = useProductTypeColumns();
  const { table } = useDataTable({
    data: product_types || [],
    columns,
    count,
    enablePagination: true,
    enableRowSelection: true,
    getRowId: (row) => row.id,
    rowSelection: {
      state: rowSelection,
      updater
    },
    pageSize: PAGE_SIZE,
    prefix: PREFIX_PRODUCT_TYPE
  });
  if (isError) {
    throw error;
  }
  return (0, import_jsx_runtime2.jsx)(
    _DataTable,
    {
      table,
      columns,
      pageSize: PAGE_SIZE,
      count,
      isLoading,
      filters,
      orderBy: [
        { key: "value", label: t("fields.value") },
        { key: "created_at", label: t("fields.createdAt") },
        { key: "updated_at", label: t("fields.updatedAt") }
      ],
      layout: "fill",
      pagination: true,
      search: true,
      prefix: PREFIX_PRODUCT_TYPE,
      queryObject: raw
    }
  );
};
var ptColumnHelper = createColumnHelper();
var useProductTypeColumns = () => {
  const base = useProductTypeTableColumns();
  return (0, import_react.useMemo)(
    () => [
      ptColumnHelper.display({
        id: "select",
        header: ({ table }) => {
          return (0, import_jsx_runtime2.jsx)(
            Checkbox,
            {
              checked: table.getIsSomePageRowsSelected() ? "indeterminate" : table.getIsAllPageRowsSelected(),
              onCheckedChange: (value) => table.toggleAllPageRowsSelected(!!value)
            }
          );
        },
        cell: ({ row }) => {
          return (0, import_jsx_runtime2.jsx)(
            Checkbox,
            {
              checked: row.getIsSelected(),
              onCheckedChange: (value) => row.toggleSelected(!!value),
              onClick: (e) => {
                e.stopPropagation();
              }
            }
          );
        }
      }),
      ...base
    ],
    [base]
  );
};
var PREFIX_SHIPPING_OPTION = "so";
var ShippingOptionTable = ({
  initialRowState,
  intermediate,
  setIntermediate
}) => {
  const { t } = useTranslation();
  const [rowSelection, setRowSelection] = (0, import_react.useState)(initialRowState);
  useCleanupSearchParams();
  const { searchParams, raw } = useShippingOptionTableQuery({
    pageSize: PAGE_SIZE,
    prefix: PREFIX_SHIPPING_OPTION
  });
  const { shipping_options, count, isLoading, isError, error } = useShippingOptions(
    {
      ...searchParams,
      fields: "+service_zone.fulfillment_set.location.*"
    },
    {
      placeholderData: keepPreviousData
    }
  );
  const updater = (value) => {
    const state = typeof value === "function" ? value(rowSelection) : value;
    const currentIds = Object.keys(rowSelection);
    const ids = Object.keys(state);
    const newIds = ids.filter((id) => !currentIds.includes(id));
    const removedIds = currentIds.filter((id) => !ids.includes(id));
    const newShippingOptions = (shipping_options == null ? void 0 : shipping_options.filter((p) => newIds.includes(p.id)).map((p) => ({
      value: p.id,
      label: p.name
    }))) || [];
    const filteredIntermediate = intermediate.filter(
      (p) => !removedIds.includes(p.value)
    );
    setIntermediate([...filteredIntermediate, ...newShippingOptions]);
    setRowSelection(state);
  };
  const { stock_locations } = useStockLocations({
    limit: 1e3
  });
  const filters = useShippingOptionTableFilters(stock_locations || []);
  const columns = useShippingOptionColumns();
  const { table } = useDataTable({
    data: shipping_options || [],
    columns,
    count,
    enablePagination: true,
    enableRowSelection: true,
    getRowId: (row) => row.id,
    rowSelection: {
      state: rowSelection,
      updater
    },
    pageSize: PAGE_SIZE,
    prefix: PREFIX_SHIPPING_OPTION
  });
  if (isError) {
    throw error;
  }
  return (0, import_jsx_runtime2.jsx)(
    _DataTable,
    {
      table,
      columns,
      pageSize: PAGE_SIZE,
      count,
      isLoading,
      filters,
      orderBy: [
        { key: "name", label: t("fields.name") },
        { key: "created_at", label: t("fields.createdAt") },
        { key: "updated_at", label: t("fields.updatedAt") }
      ],
      layout: "fill",
      pagination: true,
      search: true,
      prefix: PREFIX_SHIPPING_OPTION,
      queryObject: raw
    }
  );
};
var soColumnHelper = createColumnHelper();
var useShippingOptionColumns = () => {
  const base = useShippingOptionTableColumns();
  return (0, import_react.useMemo)(
    () => [
      soColumnHelper.display({
        id: "select",
        header: ({ table }) => {
          return (0, import_jsx_runtime2.jsx)(
            Checkbox,
            {
              checked: table.getIsSomePageRowsSelected() ? "indeterminate" : table.getIsAllPageRowsSelected(),
              onCheckedChange: (value) => table.toggleAllPageRowsSelected(!!value)
            }
          );
        },
        cell: ({ row }) => {
          return (0, import_jsx_runtime2.jsx)(
            Checkbox,
            {
              checked: row.getIsSelected(),
              onCheckedChange: (value) => row.toggleSelected(!!value),
              onClick: (e) => {
                e.stopPropagation();
              }
            }
          );
        }
      }),
      ...base
    ],
    [base]
  );
};
var ptagColumnHelper = createColumnHelper();
var useCleanupSearchParams = () => {
  const [_, setSearchParams] = useSearchParams();
  (0, import_react.useEffect)(() => {
    return () => {
      setSearchParams({});
    };
  }, []);
};
var TargetItem = ({
  index,
  label,
  onRemove,
  value
}) => {
  const { product } = useProduct(
    value,
    {
      // TODO: Remove exclusion once we avoid including unnecessary relations by default in the query config
      fields: "id,title,-type,-collection,-options,-tags,-images,-variants,-sales_channels"
    },
    { enabled: !label }
  );
  return (0, import_jsx_runtime3.jsxs)("div", { className: "bg-ui-bg-field-component shadow-borders-base flex items-center justify-between gap-2 rounded-md px-2 py-0.5", children: [
    (0, import_jsx_runtime3.jsx)(Text, { size: "small", leading: "compact", children: label || (product == null ? void 0 : product.title) }),
    (0, import_jsx_runtime3.jsx)(
      IconButton,
      {
        size: "small",
        variant: "transparent",
        type: "button",
        onClick: () => onRemove(index),
        children: (0, import_jsx_runtime3.jsx)(XMarkMini, {})
      }
    )
  ] });
};
var TaxRateRuleReferenceSchema = external_exports.object({
  value: external_exports.string(),
  label: external_exports.string()
});
var TaxRateRuleTargetSchema = external_exports.object({
  reference_type: external_exports.nativeEnum(TaxRateRuleReferenceType),
  references: external_exports.array(TaxRateRuleReferenceSchema)
});
var createTaxRulePayload = (target) => {
  return target.references.map((reference) => ({
    reference: target.reference_type,
    reference_id: reference.value
  }));
};

export {
  TaxRateRuleReferenceType,
  TargetForm,
  TargetItem,
  TaxRateRuleReferenceSchema,
  createTaxRulePayload
};
//# sourceMappingURL=chunk-QJHGGYMT.js.map
